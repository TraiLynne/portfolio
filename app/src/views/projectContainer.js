import React from 'react';
import {
    ProjectCarousel
} from '../components';

export default function ProjectView(props){
    return (
        <div id='project-container'>
            <ProjectCarousel/>
        </div>
    )
}